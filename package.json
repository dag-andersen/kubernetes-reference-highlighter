{
  "name": "k8s-checker",
  "displayName": "k8s-checker",
  "description": "",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.71.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "k8s-checker.enableWorkSpaceCrawling",
        "title": "k8s-checker: Toggle WorkSpace Crawling"
      },
      {
        "command": "k8s-checker.enableClusterCrawling",
        "title": "k8s-checker: Toggle Cluster Crawling"
      },
      {
        "command": "k8s-checker.enableKustomizeCrawling",
        "title": "k8s-checker: Toggle Kustomize Crawling"
      }
    ],
    "semanticTokenTypes": [
      {
        "id": "templateType",
        "superType": "type",
        "description": "A template type."
      }
    ],
    "semanticTokenModifiers": [
      {
        "id": "native",
        "description": "Annotates a symbol that is implemented natively"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js"
  },
  "devDependencies": {
    "@types/glob": "^7.2.0",
    "@types/mocha": "^9.1.1",
    "@types/node": "16.x",
    "@types/vscode": "^1.71.0",
    "@typescript-eslint/eslint-plugin": "^5.31.0",
    "@typescript-eslint/parser": "^5.31.0",
    "@vscode/test-electron": "^2.1.5",
    "eslint": "^8.20.0",
    "glob": "^8.0.3",
    "mocha": "^10.0.0",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "@kubernetes/client-node": "^0.17.1",
    "js-yaml": "^4.1.0",
    "yaml": "^2.1.1"
  }
}
